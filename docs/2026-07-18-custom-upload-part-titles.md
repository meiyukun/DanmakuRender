# 上传分P名称自定义

- 上传中心的分P顺序列表为每个视频增加独立名称输入框，默认使用素材标题或源文件名。
- 分P名称在翻页、筛选和调整顺序时保留，移除素材时同步清理。
- 提交接口新增 `parts: [{path, title}]` 数据结构，同时兼容原有的 `paths` 列表。
- 后端校验每个分P名称为1至80个字符，并通过 `biliwebapi` 写入B站分P标题。
- 新投稿和追加已有稿件均支持自定义分P名称。

新增文件：

- `docs/2026-07-18-custom-upload-part-titles.md`
