# 热点素材多区间跳剪

- v3 虚拟热点片段新增 `ranges`，一个素材可按直播时间顺序保留多个不连续区间。
- `pieces` 增加 `range_index`，同时表达人为跳剪和跨录像分段映射。
- 精剪弹窗新增排除中段、待排除预览、排除列表、撤销和清空操作。
- 完整选区预览、混剪和上传实体化会自动跳过排除部分，一个热点仍作为一个素材使用。
- 素材时长改为所有保留区间之和，缩略图使用首个保留区间，封面字幕上下文忽略被排除内容。
- 旧 v3 单区间清单和旧 `start/end` 保存请求继续兼容。

本次改动没有新增配置项或运行期文件。
