# 热点边界审片优化

- 修复精剪滑块在初始化和调整视野时被浏览器夹紧，导致首次范围错误或重新打开后显示为 1 秒的问题。
- 分离算法范围、最近已保存范围和本次未保存范围，并增加“恢复已保存范围”。
- 增加 0.5× 至 3× 倍速、前进/后退 5 秒、起点预览、终点前 5 秒预览和精确时间输入。
- 拖动滑块、点击边界微调按钮或以当前画面设置边界后，会从调整后的边界点开始播放。
- 增加 J/L、I/O、B/E 等审片快捷键、未保存状态和关闭确认。
- 跨录像分段的定位、快进、边界播放和终点预览继续使用整场全局时间轴。

本次改动没有新增配置项或运行期文件。
