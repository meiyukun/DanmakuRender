# AGENTS.md

## 协作规则

- 保留用户已有的未提交改动；不要覆盖或提交与当前任务无关的文件。
- 修改 `configs/`、`.login_info/`、`logs/`、`.temp/` 前，先确认操作目的；这些目录可能包含本机状态或私有数据。
- 不要提交账号信息、cookies、日志、临时文件或真实任务配置中的隐私内容。
- 文件路径和用户可见文本中已有大量中文，编辑时保持 UTF-8。

## 代码维护

- 沿用项目既有的线程、队列、插件和任务消息传递结构。
- YAML 配置默认值及合并逻辑集中在 `DMR/Config/__init__.py`；不要在业务模块重复实现。
- 新增插件或任务事件时，同时检查 `DMR/engine.py` 的 `pipeSend` 路由及相应插件的队列消费逻辑。

## 验证与副作用

- 配置解析改动至少运行 `python main.py --version`；再按改动范围做目标验证。
- 录制、上传和清理会产生外部副作用。运行前确认任务配置、输出目录、上传延迟和清理策略。
- `dryrun.py` 仍可能触发录制和上传逻辑，谨慎执行。

## 功能变更记录

- 每次新增功能时，都要在 `docs/` 中新增一份日志摘要，说明本次改动内容及新增或生成的文件。
